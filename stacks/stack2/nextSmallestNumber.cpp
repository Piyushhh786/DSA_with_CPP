#include<iostream>
#include<stack>
#include<vector>
using namespace std;
vector<int> nSEI(vector<int>arr){
    int n=arr.size();
    vector<int>ans(n);
    stack<int>st;


}
int main(){
    int arr[]={2,1,5,6,2,3};
    int n=sizeof(arr)/4;
    vector<int>v(arr,arr+n);
    v=nSEI(v);    
}